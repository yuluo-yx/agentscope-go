# Loop Basic Example

This example attaches a report-only Loop Engineering spec to an Agent. It uses a DashScope ChatModel, so set `AI_DASHSCOPE_API_KEY` before running it.

Run:

```bash
go run .
```
