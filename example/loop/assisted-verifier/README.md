# Assisted Loop Verifier Example

This example attaches an assisted Loop Engineering spec and a verifier to an Agent. It demonstrates maker/checker separation with a DashScope-backed maker Agent. Set `AI_DASHSCOPE_API_KEY` before running it.

Run:

```bash
go run .
```
