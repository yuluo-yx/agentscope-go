# Assisted Loop Verifier Example

This example attaches an assisted Loop Engineering spec and a verifier to an Agent. It demonstrates maker/checker separation without requiring a live model provider.

Run:

```bash
go run .
```
